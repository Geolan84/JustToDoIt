/// Class with server urls constants.
class AppUrls {
  /// Base url for server.
  static const baseUrl = 'https://beta.mrdekk.ru/todo';

  /// Url for task-list endpoints.
  static const tasksList = '$baseUrl/list';

  /// Url for single task endpoints.
  static const singleTask = '$tasksList/';
}
